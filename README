To install, use:

    $ sudo apt-get install curl python-scipy python-matplotlib python-tables firefox
    $ python setup.py download_models
    $ sudo python setup.py install

To test the recognizer, run:

    $ ./run-test
